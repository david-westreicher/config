order += "volume_master"
order += "disk /"
order += "disk /home"
order += "time"

volume master {
    format = "🔊 %volume"
    format_muted = "🔇"
    device = "pulse"
}

disk "/" {
    format = '💿 %avail'
}


disk "/home" {
    format = '📀 %avail'
}

time {
    format = '📆 %d-%m-%Y 🕐 %H:%M:%S'
}
